[general]
model = "vitl16_sat"   # see DINO_MODELS in main.py for options
stride = 1
downsample_factor = 2
subtract_pos = false
seed = 42
figures_dir = "figures"
tasks = [2,]  # 1=download, 2=PCA, 3=retrieval+figures, 4=model comparison

[task2]
n_images = 4
dense = true

[task3]
query_ds = "kidney"
target_ds = "kidney"
n_targets = 7
