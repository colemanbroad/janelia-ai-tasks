[general]
model = "vitl16_sat"   # see DINO_MODELS in main.py for options
# model = "vitb16"
# model = "vith16plus"
stride = 4
downsample_factor = 2
subtract_pos = false
subtract_pos_empirical = true  # subtract per-pixel mean across all images in task2
seed = 42
figures_dir = "figures"
tasks = [3,] # 1=download, 2=PCA, 3=retrieval+figures, 4=model comparison

[task2]
n_images = 9

[task3]
n_targets = 4
